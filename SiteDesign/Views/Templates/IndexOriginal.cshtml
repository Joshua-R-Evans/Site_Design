@model IEnumerable<SiteDesign.Models.Template>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>

    <table class="table" id="whiteback">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FolderName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.HtmlFile)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CssFile)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Image)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SiteUser.Email)
            </th>
            <th></th>
        </tr>
@foreach (var item in Model)
{

    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FolderName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.HtmlFile)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CssFile)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Image)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SiteUser.Email)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.TemplateId }) |
            @Html.ActionLink("Details", "Details", new { id = item.TemplateId }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.TemplateId })
        </td>
    </tr>

}

</table>
